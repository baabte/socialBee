describe('forgotPassword', function() {

	beforeEach(module('touterbee'));

	it('should ...', inject(function($controller) {

        var controller = $controller('forgotPassword');

		expect(controller('input')).toEqual('output');

	}));

});