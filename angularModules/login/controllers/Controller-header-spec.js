describe('header', function() {

	beforeEach(module('touterbee'));

	it('should ...', inject(function($controller) {

        var controller = $controller('header');

		expect(controller('input')).toEqual('output');

	}));

});