describe('domainVerification', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(domainVerification) {

	//expect(domainVerification.doSomething()).toEqual('something');

  }));

});